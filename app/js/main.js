$(function(){
   $('.top-slider__inner').slick({
    dots: true,
    arrows: false,
    fade: true,
    autoplay: true,
    autoplaySpeed: 1800,
    speed: 1500
   });

   var mixer = mixitup('.products-content');
   var mixer = mixitup('.products-button');
});